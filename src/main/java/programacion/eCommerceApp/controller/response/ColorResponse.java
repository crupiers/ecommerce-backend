package programacion.eCommerceApp.controller.response;

public record ColorResponse(String nombre, int estado) {
    //voy a pasar todos los parametros que quiera mostrar (en este caso, nombre y estado)
}
